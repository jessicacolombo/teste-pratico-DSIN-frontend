import styled from "styled-components";

export const StyledLiAdmin = styled.li`
  list-style: none;
  display: flex;
  justify-content: space-evenly;
  flex-direction: column;
  gap: 5px;
  border: 1px dashed;
  padding: 7px;
  border-radius: var(--border-radius);
  cursor: pointer;
`;
